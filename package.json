{
  "license": "UNLICENSED",
  "scripts": {
    "ui:format": "yarn --cwd front-ui lint-staged",
    "ui:test-master": "yarn --cwd front-ui test --changedSince=origin/master --watchAll=false",
    "ui:test-current": "yarn --cwd front-ui test --changedSince=@{push} --watchAll=false",
    "api:format": "yarn --cwd api-server lint-staged",
    "api:test": "yarn --cwd api-server test --watchAll=false",
    "prepare": "husky install"
  },
  "dependencies": {
    "husky": "^7.0.4"
  }
}
